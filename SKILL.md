---
name: douni-weekly-viral-video-digest
description: 每周复盘豆包用增各机构上周一至周日发布的全部视频，优先把最新互动写入原抖音、小红书及独立端客户发布主账并同步机构表，既按抖音万赞、小红书千赞筛选爆款，也筛选互动总和低于 100 的作品并按机构归类，以两张紧凑飞书卡片私聊测试或发送焦尾-豆包用增群。用于“每周爆款复盘、上周爆款视频、低互动达人、刷新上周互动、万赞/千赞视频汇总”等请求。
---

# 每周爆款视频复盘

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douni-weekly-viral-video-digest
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 目标

每周一复盘上一自然周各机构的真实发布视频。发布事实以原抖音、小红书客户发布表和新【独立端-内容发布】为准；机构表只用于独立端归属消歧和互动镜像，不反向推测发布事实。

## 固定口径

1. 时间窗口固定为上周一 00:00 至上周日 23:59（上海时区）。
2. 来源包括【视频发布（一口价）】、【小红书视频发布】和新表【独立端-内容发布】。双端专项中 PC 端仍在原工作任务来源，独立端才进入新表；不能合并为一条。
3. 小红书只统计“内容形式”包含“视频”的记录，图文不计入视频总数和爆款。
4. 原客户表机构归属读取末列机构色块；自建读取文字。独立端表没有机构列，必须反查 13 张机构表，以作品/稿件/主页/达人并限定“独立端+PC端”专项唯一确认；识别不到时标为“机构待确认”，不得猜测。
5. 客户发布表是互动数据主账，包括原抖音、小红书和独立端三个已登记来源。实时抓取后，必须先写入作品所属客户发布表并回读校验；独立端不能误写到原抖音表。只有客户表确认成功的记录才继续同步到达人所属机构执行表。机构表失败不得阻塞或回滚客户主表，只登记待核。手动补采、重登录后的恢复执行同样遵守，不能只生成卡片或只回填机构。
6. 抖音公开互动必须在本轮重新抓取赞、评、藏、转发，先更新客户发布表，再同步机构表；互动总和为四项相加。机构表按发布链接或 GID 唯一定位，最后才允许用达人名称 + 发布时间 + 平台的唯一组合兜底，无法唯一定位时不得猜行。
7. 小红书于 `2026-08-31` 至 `2026-09-07` 灰度开启实时互动刷新：仅在每周复盘中运行一次，串行访问（并发数 1），先写客户发布表并回读，再同步机构表。单条失败保留原值，不重试轰炸；试用期后自动回到客户发布表互动快照，客户表已有快照仍可同步给机构表，是否长期开放由遥师复核。
8. 爆款阈值：抖音点赞大于等于 10,000；小红书点赞大于等于 1,000。
9. 低互动阈值：互动总和有值且小于 100。互动总和为空的不判为低互动，单独统计数据缺失数量；不得把空值当 0。
10. 单条作品公开数据缺失或机构表无法唯一定位不阻断整批；保留原值并在执行结果登记需复核数量。不得用估算值补齐。
11. 原抖音、小红书发布源直接确定平台；不能因小红书没有“平台”列而默认归入抖音。独立端按明确的平台或作品链接识别，无法识别时停止该记录的路由。
12. 抓取使用仓库 MediaCrawler 虚拟环境的 Python，启动前检查可执行文件；不得依赖未验证的 `uv` 或 PATH。整批抓取失败按实际受影响作品计数，不能把两次批次失败写成两条视频失败。互动未查全时只报告已确认的爆款，不得宣称“上周没有爆款”；飞书授权与内容平台登录分别判断。

## 已核实互动的补写恢复

常规定时入口仍为仓库 `scripts/run-weekly-viral-video-digest.mjs`，客户表先写回并回读、机构后同步的顺序不变。若浏览器补采已经形成含 `matched`（每条含来源、作品链接及完整 `metrics`）和 `missing` 的核实结果 JSON，不要为回填再次采集平台：

```powershell
node scripts/backfill-verified-weekly-customer-metrics.mjs --input <核实结果.json>
node scripts/backfill-verified-weekly-customer-metrics.mjs --input <核实结果.json> --apply
```

第一条只预览；得到用户本次客户互动写入授权后再用 `--apply`。只接受 24 小时内完整、非负且总和一致的实测数据，按当前客户表作品链接及达人复核定位，不沿用旧行号；有歧义不写，不补零，不新增发布记录。只更新赞、评、藏、转发及互动总和，保留已有公式。默认在输入旁生成 `customer-backfill-applied.json`，须检查 `ok`、各客户源 `verified` 与 `needsReview`，不能把机构回执冒充客户回执。机构已回填的记录不必再写；未回填的，只将客户回执确认成功的记录交给原机构互动执行器。此恢复命令不发送卡片、不变更调度，数据过期时重新核实而非放宽日期检查。

## 卡片交付

同一次周一任务发送两张 Card 2.0 卡片：

第一张为蓝色标题栏的爆款复盘卡：

- 标题：正式版“上周爆款复盘”；试跑版“上周爆款复盘（测试）”。
- 指标区：上周视频总数、爆款总数、抖音/小红书爆款数。
- 口径区：阈值、原抖音/小红书/独立端客户发布表各自写入并回读数量、机构表同步/核对数量，以及抖音、小红书各自的实时刷新或快照状态。
- 明细区：每条一行作品超链接，下一行展示机构、平台、赞、评、藏、转发、互动总和。
- 按点赞数从高到低排序。

第二张为橙色标题栏的“上周低互动关注”卡：

- 指标区：低互动作品数、涉及机构数、互动数据缺失数。
- 口径区：只统计互动总和有值且低于 100 的作品，并明确空值不判低互动。
- 明细区：按机构分类，每条展示可点击达人/作品链接、平台和互动总和；同机构内按互动总和从低到高排序。
- 必须同时交付独立的《低互动明细》飞书表，卡片放明显的“打开低互动明细表”入口，不以折叠名单替代表格。明细按机构、互动总和排序，保留每期作品，不按同名合并；至少包含机构、达人、平台、发布日期、作品链接、赞评藏转、互动总和、客户发布表入口和数据时间。机构唯一 ID 只有可靠来源已提供时才列出，不猜填。
- 明细表直接复用本轮已核实结果，不为建表重新抓平台、不重复回填客户或机构互动。数字按数值写入，空值不补 0；建表后逐行回读，核对完整性、各机构数量和卡片总数。互动缺失单独说明，不混入低互动名单。
- 同一周、同一结果复用已验证明细表；本地报告与建表回执保存在 `data/weekly-low-engagement-reports`，不作为缓存清理。创建响应不确定时先找回并验证已建表，不盲目重建；已有 URL 时仅续做回读或卡片交付。报告表不自动开放公开权限，发群沿用现有发送确认。
- 没有低互动作品时仍发送卡片并明确写“0 条”，避免同事误以为任务漏跑。

## 只补低互动明细表

已完成取数但漏交明细表时，使用原结果（正式 report 或包含 low/missing 的复核 JSON）：

```powershell
node scripts/publish-weekly-low-engagement-detail.mjs --input <原结果.json>
node scripts/publish-weekly-low-engagement-detail.mjs --input <原结果.json> --apply
```

先预览条数、机构和明细，再按用户本次授权建报告表。此入口只建表/回读，不抓取、不回填客户或机构、不发送消息；原卡片需经确认后用已验证 URL 补入口，保留原消息、不重复发爆款。若创建状态不确定，先人工核实并恢复回执中的 URL，不删除回执绕过防重建。

## 发送边界

- 首次上线前只私聊遥师，不发群。
- 新上线或变更外发范围仍需遥师确认；已上线任务按 Daemon 现有启停和时间执行，发送到已配置内部群，不因重新读取本 Skill 而重置启停。
- 爆款卡与低互动卡分别使用独立幂等键；试跑和正式发送也使用不同幂等键，同一周同一结果不得重复发送。

## 验收

1. 上周视频总数按全部已登记当前客户发布源取数，包含独立端，经平台和作品证据去重后计算；不能只合计原两张表而漏掉独立端。
2. 每条爆款达到对应平台阈值，链接可点击，机构归属可追溯。
3. 作品所属客户发布表是互动数据第一落点并完成回读校验（含独立端）；只有客户表确认成功的记录才同步机构表，机构表失败不影响客户主账。补采恢复须有客户写入回执，不能只报告机构更新或卡片发送成功。
4. 新任务试跑不得自行上线；已启用任务的单次预览不改变现有调度状态。
5. 低互动卡中的每条记录互动总和必须有值且小于 100，并按机构归类；互动为空的记录只计入缺失数，不进入明细。
6. 低互动卡必须带已回读验证的明细表入口，表内低互动条数与卡片一致；建表或校验失败不声称“已附表”，保留本轮报告供续作，不重复抓取。
